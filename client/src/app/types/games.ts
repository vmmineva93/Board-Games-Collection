export interface Game {
    _ownerId: string;
    title: string,
    players: string,
    playingTime: string,
    age: string,
    categories: string,
    description: string,
    imageUrl: string,
    _createdOn: string,
    _id: string;
}